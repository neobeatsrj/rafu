"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Server = exports.defaultOnError = void 0;
const http_1 = require("http");
const parseUrl_1 = require("./parseUrl");
const Router_1 = require("./Router");
const dropComponents_1 = require("./dropComponents");
const defaultOnError = (e, req, res) => {
    res.statusCode = e.code || e.status || 500;
    res.setHeader('Content-Type', 'text/plain');
    if (typeof e === 'string') {
        res.end(e);
    }
    else {
        res.end(e.message || http_1.STATUS_CODES[res.statusCode] || 'Unknown error');
    }
};
exports.defaultOnError = defaultOnError;
class Server extends Router_1.Router {
    _httpServer;
    _onError;
    _on404;
    constructor({ server, onError } = {}) {
        super();
        this._httpServer = server;
        this._onError = onError || exports.defaultOnError;
        this._on404 = this._onError.bind(null, { code: 404 });
    }
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    async listen(...args) {
        return new Promise((resolve, reject) => {
            if (!this._httpServer) {
                this._httpServer = (0, http_1.createServer)();
            }
            this._httpServer.on('request', this.handleRequest);
            this._httpServer.listen.apply(this._httpServer, [
                ...args,
                (e) => {
                    if (e) {
                        reject(e);
                    }
                    else {
                        resolve(this);
                    }
                }
            ]);
        });
    }
    async close() {
        return new Promise((resolve, reject) => {
            if (!this._httpServer) {
                reject('Server not open when trying to close');
                return;
            }
            this._httpServer.close(e => {
                if (e) {
                    reject(e);
                }
                else {
                    resolve();
                }
            });
        });
    }
    use(pathOrFn, ...fns) {
        const path = (typeof pathOrFn === 'string' && pathOrFn) || '/';
        const callbacks = typeof pathOrFn === 'string' ? fns : [pathOrFn, ...fns];
        return super.use(path, ...callbacks);
    }
    handleRequest = (req, res) => {
        const parsedUrl = (0, parseUrl_1.parseUrl)(req);
        const foundRoutes = this.find(req.method, parsedUrl.pathname);
        foundRoutes.push({
            callback: this._on404,
            params: {},
            componentsToDrop: 0
        });
        const routeCount = foundRoutes.length;
        req.path = parsedUrl.pathname;
        req.query = parsedUrl.query;
        req.search = parsedUrl.search;
        let i = 0;
        let loop;
        const next = e => {
            if (e) {
                this._onError(e, req, res, next);
            }
            else {
                loop();
            }
        };
        try {
            loop = () => {
                if (!res.writableEnded) {
                    if (i >= routeCount) {
                        return;
                    }
                    const foundRoute = foundRoutes[i++];
                    req.path = (0, dropComponents_1.dropComponents)(parsedUrl.pathname, foundRoute.componentsToDrop);
                    req.param = req.params = { ...req.params, ...foundRoute.params };
                    const callback = foundRoute.callback;
                    callback(req, res, next);
                }
            };
            loop();
        }
        catch (e) {
            this._onError(e, req, res, next);
        }
    };
}
exports.Server = Server;
//# sourceMappingURL=Server.js.map