import { __decorate } from "tslib";
import { rtfm } from '@twurple/common';
import { EventSubChannelCharityDonationEvent } from '../events/EventSubChannelCharityDonationEvent.js';
import { EventSubSubscription } from './EventSubSubscription.js';
/** @internal */
let EventSubChannelCharityDonationSubscription = class EventSubChannelCharityDonationSubscription extends EventSubSubscription {
    _userId;
    /** @protected */ _cliName = 'charity-donation';
    constructor(handler, client, _userId) {
        super(handler, client);
        this._userId = _userId;
    }
    get id() {
        return `channel.charity_campaign.donate.${this._userId}`;
    }
    get authUserId() {
        return this._userId;
    }
    transformData(data) {
        return this._client._config.managed
            ? new EventSubChannelCharityDonationEvent(data, this._client._config.apiClient)
            : new EventSubChannelCharityDonationEvent(data);
    }
    async _subscribe() {
        return this._client._config.managed
            ? await this._client._config.apiClient.eventSub.subscribeToChannelCharityDonationEvents(this._userId, await this._getTransportOptions())
            : undefined;
    }
};
EventSubChannelCharityDonationSubscription = __decorate([
    rtfm('eventsub-base', 'EventSubSubscription')
], EventSubChannelCharityDonationSubscription);
export { EventSubChannelCharityDonationSubscription };
