import { __decorate } from "tslib";
import { rtfm } from '@twurple/common';
import { EventSubChannelUpdateEvent } from '../events/EventSubChannelUpdateEvent.js';
import { EventSubSubscription } from './EventSubSubscription.js';
/** @internal */
let EventSubChannelUpdateSubscription = class EventSubChannelUpdateSubscription extends EventSubSubscription {
    _userId;
    /** @protected */ _cliName = 'stream-change';
    constructor(handler, client, _userId) {
        super(handler, client);
        this._userId = _userId;
    }
    get id() {
        return `channel.update.v2.${this._userId}`;
    }
    get authUserId() {
        return this._userId;
    }
    transformData(data) {
        return this._client._config.managed
            ? new EventSubChannelUpdateEvent(data, this._client._config.apiClient)
            : new EventSubChannelUpdateEvent(data);
    }
    async _subscribe() {
        return this._client._config.managed
            ? await this._client._config.apiClient.eventSub.subscribeToChannelUpdateEvents(this._userId, await this._getTransportOptions())
            : undefined;
    }
};
EventSubChannelUpdateSubscription = __decorate([
    rtfm('eventsub-base', 'EventSubSubscription')
], EventSubChannelUpdateSubscription);
export { EventSubChannelUpdateSubscription };
