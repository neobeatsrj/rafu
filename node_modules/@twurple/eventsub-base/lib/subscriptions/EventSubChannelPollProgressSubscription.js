import { __decorate } from "tslib";
import { rtfm } from '@twurple/common';
import { EventSubChannelPollProgressEvent } from '../events/EventSubChannelPollProgressEvent.js';
import { EventSubSubscription } from './EventSubSubscription.js';
/** @internal */
let EventSubChannelPollProgressSubscription = class EventSubChannelPollProgressSubscription extends EventSubSubscription {
    _userId;
    /** @protected */ _cliName = 'poll-progress';
    constructor(handler, client, _userId) {
        super(handler, client);
        this._userId = _userId;
    }
    get id() {
        return `channel.poll.progress.${this._userId}`;
    }
    get authUserId() {
        return this._userId;
    }
    transformData(data) {
        return this._client._config.managed
            ? new EventSubChannelPollProgressEvent(data, this._client._config.apiClient)
            : new EventSubChannelPollProgressEvent(data);
    }
    async _subscribe() {
        return this._client._config.managed
            ? await this._client._config.apiClient.eventSub.subscribeToChannelPollProgressEvents(this._userId, await this._getTransportOptions())
            : undefined;
    }
};
EventSubChannelPollProgressSubscription = __decorate([
    rtfm('eventsub-base', 'EventSubSubscription')
], EventSubChannelPollProgressSubscription);
export { EventSubChannelPollProgressSubscription };
