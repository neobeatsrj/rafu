import { __decorate } from "tslib";
import { rtfm } from '@twurple/common';
import { EventSubAutoModTermsUpdateEvent } from '../events/EventSubAutoModTermsUpdateEvent.js';
import { EventSubSubscription } from './EventSubSubscription.js';
/** @internal */
let EventSubAutoModTermsUpdateSubscription = class EventSubAutoModTermsUpdateSubscription extends EventSubSubscription {
    _broadcasterId;
    _moderatorId;
    /** @protected */ _cliName = '';
    constructor(handler, client, _broadcasterId, _moderatorId) {
        super(handler, client);
        this._broadcasterId = _broadcasterId;
        this._moderatorId = _moderatorId;
    }
    get id() {
        return `automod.terms.update.${this._broadcasterId}.${this._moderatorId}`;
    }
    get authUserId() {
        return this._moderatorId;
    }
    transformData(data) {
        return this._client._config.managed
            ? new EventSubAutoModTermsUpdateEvent(data, this._client._config.apiClient)
            : new EventSubAutoModTermsUpdateEvent(data);
    }
    async _subscribe() {
        return this._client._config.managed
            ? await this._client._config.apiClient.asUser(this._moderatorId, async (ctx) => await ctx.eventSub.subscribeToAutoModTermsUpdateEvents(this._broadcasterId, await this._getTransportOptions()))
            : undefined;
    }
};
EventSubAutoModTermsUpdateSubscription = __decorate([
    rtfm('eventsub-base', 'EventSubSubscription')
], EventSubAutoModTermsUpdateSubscription);
export { EventSubAutoModTermsUpdateSubscription };
