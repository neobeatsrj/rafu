import { __decorate } from "tslib";
import { rtfm } from '@twurple/common';
import { EventSubChannelUnbanEvent } from '../events/EventSubChannelUnbanEvent.js';
import { EventSubSubscription } from './EventSubSubscription.js';
/** @internal */
let EventSubChannelUnbanSubscription = class EventSubChannelUnbanSubscription extends EventSubSubscription {
    _userId;
    /** @protected */ _cliName = 'unban';
    constructor(handler, client, _userId) {
        super(handler, client);
        this._userId = _userId;
    }
    get id() {
        return `channel.unban.${this._userId}`;
    }
    get authUserId() {
        return this._userId;
    }
    transformData(data) {
        return this._client._config.managed
            ? new EventSubChannelUnbanEvent(data, this._client._config.apiClient)
            : new EventSubChannelUnbanEvent(data);
    }
    async _subscribe() {
        return this._client._config.managed
            ? await this._client._config.apiClient.eventSub.subscribeToChannelUnbanEvents(this._userId, await this._getTransportOptions())
            : undefined;
    }
};
EventSubChannelUnbanSubscription = __decorate([
    rtfm('eventsub-base', 'EventSubSubscription')
], EventSubChannelUnbanSubscription);
export { EventSubChannelUnbanSubscription };
