import { __decorate } from "tslib";
import { rtfm } from '@twurple/common';
import { EventSubChannelSuspiciousUserUpdateEvent } from '../events/EventSubChannelSuspiciousUserUpdateEvent.js';
import { EventSubSubscription } from './EventSubSubscription.js';
/** @internal */
let EventSubChannelSuspiciousUserUpdateSubscription = class EventSubChannelSuspiciousUserUpdateSubscription extends EventSubSubscription {
    _broadcasterId;
    _moderatorId;
    /** @protected */ _cliName = '';
    constructor(handler, client, _broadcasterId, _moderatorId) {
        super(handler, client);
        this._broadcasterId = _broadcasterId;
        this._moderatorId = _moderatorId;
    }
    get id() {
        return `channel.suspicious_user.update.${this._broadcasterId}.${this._moderatorId}`;
    }
    get authUserId() {
        return this._moderatorId;
    }
    transformData(data) {
        return this._client._config.managed
            ? new EventSubChannelSuspiciousUserUpdateEvent(data, this._client._config.apiClient)
            : new EventSubChannelSuspiciousUserUpdateEvent(data);
    }
    async _subscribe() {
        return this._client._config.managed
            ? await this._client._config.apiClient.asUser(this._moderatorId, async (ctx) => await ctx.eventSub.subscribeToChannelSuspiciousUserUpdateEvents(this._broadcasterId, await this._getTransportOptions()))
            : undefined;
    }
};
EventSubChannelSuspiciousUserUpdateSubscription = __decorate([
    rtfm('eventsub-base', 'EventSubSubscription')
], EventSubChannelSuspiciousUserUpdateSubscription);
export { EventSubChannelSuspiciousUserUpdateSubscription };
