import { __decorate } from "tslib";
import { rtfm } from '@twurple/common';
import { EventSubChannelBanEvent } from '../events/EventSubChannelBanEvent.js';
import { EventSubSubscription } from './EventSubSubscription.js';
/** @internal */
let EventSubChannelBanSubscription = class EventSubChannelBanSubscription extends EventSubSubscription {
    _userId;
    /** @protected */ _cliName = 'ban';
    constructor(handler, client, _userId) {
        super(handler, client);
        this._userId = _userId;
    }
    get id() {
        return `channel.ban.${this._userId}`;
    }
    get authUserId() {
        return this._userId;
    }
    transformData(data) {
        return this._client._config.managed
            ? new EventSubChannelBanEvent(data, this._client._config.apiClient)
            : new EventSubChannelBanEvent(data);
    }
    async _subscribe() {
        return this._client._config.managed
            ? await this._client._config.apiClient.eventSub.subscribeToChannelBanEvents(this._userId, await this._getTransportOptions())
            : undefined;
    }
};
EventSubChannelBanSubscription = __decorate([
    rtfm('eventsub-base', 'EventSubSubscription')
], EventSubChannelBanSubscription);
export { EventSubChannelBanSubscription };
