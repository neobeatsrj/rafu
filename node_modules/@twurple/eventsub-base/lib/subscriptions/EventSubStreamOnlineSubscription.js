import { __decorate } from "tslib";
import { rtfm } from '@twurple/common';
import { EventSubStreamOnlineEvent } from '../events/EventSubStreamOnlineEvent.js';
import { EventSubSubscription } from './EventSubSubscription.js';
/** @internal */
let EventSubStreamOnlineSubscription = class EventSubStreamOnlineSubscription extends EventSubSubscription {
    _userId;
    /** @protected */ _cliName = 'streamup';
    constructor(handler, client, _userId) {
        super(handler, client);
        this._userId = _userId;
    }
    get id() {
        return `stream.online.${this._userId}`;
    }
    get authUserId() {
        return this._userId;
    }
    transformData(data) {
        return this._client._config.managed
            ? new EventSubStreamOnlineEvent(data, this._client._config.apiClient)
            : new EventSubStreamOnlineEvent(data);
    }
    async _subscribe() {
        return this._client._config.managed
            ? await this._client._config.apiClient.eventSub.subscribeToStreamOnlineEvents(this._userId, await this._getTransportOptions())
            : undefined;
    }
};
EventSubStreamOnlineSubscription = __decorate([
    rtfm('eventsub-base', 'EventSubSubscription')
], EventSubStreamOnlineSubscription);
export { EventSubStreamOnlineSubscription };
